import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#F2BE5B', // 원하는 색상으로 수정
    },
    secondary: {
      main: '#ffffff', // 원하는 색상으로 수정
    },
  },
});

export default theme;
